"""
Web 管理平台 — 路径与配置常量

硬性约束：
  - 所有路径统一在此管理，禁止代码内硬编码
  - SQLite 数据库存放在根目录 cache/ 下，方便 Docker 挂载
  - Allure 报告存放在根目录 reports/ 下
  - 现有 pytest 项目代码不在此配置文件中引用
"""
from pathlib import Path

# ============================================================
# 项目根目录
# ============================================================
# web_backend/config.py 的上上层目录即项目根
PROJECT_ROOT = Path(__file__).resolve().parent.parent

# ============================================================
# 数据存储目录
# ============================================================
CACHE_DIR = PROJECT_ROOT / "cache"          # SQLite 数据库 + 用例缓存
REPORTS_DIR = PROJECT_ROOT / "reports"      # Allure 报告（run_tag 独立子目录）

# ============================================================
# SQLite 数据库路径
# ============================================================
DB_PATH = str(CACHE_DIR / "test_platform.db")

# ============================================================
# FastAPI 服务配置
# ============================================================
BACKEND_HOST = "0.0.0.0"
BACKEND_PORT = 8000

# 开发模式下 Vite 前端地址（用于 CORS）
VITE_DEV_URL = "http://localhost:5173"

# CORS 允许的源
CORS_ORIGINS = [
    VITE_DEV_URL,
    "http://127.0.0.1:5173",
    "http://localhost:8000",
    "http://127.0.0.1:8000",
]

# ============================================================
# pytest 执行配置
# ============================================================
PYTEST_TIMEOUT = 600               # 单个测试任务超时（秒）
# 平台访问地址（钉钉消息里的报告链接用，改成你的实际 IP）
PLATFORM_URL = "http://192.168.149.100:8001"

ENV_OPTIONS = ["dev", "staging", "prod", "docker"]  # 环境下拉选项

# ============================================================
# 初始创建目录
# ============================================================
CACHE_DIR.mkdir(parents=True, exist_ok=True)
REPORTS_DIR.mkdir(parents=True, exist_ok=True)
