# =============================================
# 若依API测试管理平台 — 后端依赖
# =============================================
# 一期核心依赖
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
aiofiles>=24.0.0
python-multipart>=0.0.12

# 二期依赖（一期暂不安装）
# python-jose[cryptography]>=3.3.0
# passlib[bcrypt]>=1.7.4

# 三期依赖（Mock平台暂不安装）
# （无额外依赖，使用标准库）
